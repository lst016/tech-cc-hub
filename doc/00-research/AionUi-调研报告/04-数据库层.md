# AionUi 调研报告 - 数据库层

**模块**: `src/process/services/database/`
**调研时间**: 2026-05-01

---

## 一、模块结构

```
database/
├── IAcpSessionRepository.ts       # ACP 会话仓库接口
├── IChannelRepository.ts         # 频道仓库接口
├── IConversationRepository.ts    # 会话仓库接口
├── SqliteAcpSessionRepository.ts # ACP 会话实现
├── SqliteChannelRepository.ts    # 频道实现
├── SqliteConversationRepository.ts  # 会话实现
├── StreamingMessageBuffer.ts     # 流式消息缓冲
├── drivers/                      # SQLite 驱动
├── export.ts                     # 导出
├── index.ts                      # 入口
├── migrations.ts                 # 迁移脚本
├── schema.ts                     # Schema 定义
└── types.ts                     # 类型定义
```

---

## 二、数据库 Schema

### 2.1 schema.ts 核心表

```sql
-- 用户表
CREATE TABLE users (
  id TEXT PRIMARY KEY,
  username TEXT UNIQUE NOT NULL,
  email TEXT UNIQUE,
  password_hash TEXT NOT NULL,
  avatar_path TEXT,
  jwt_secret TEXT,
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL,
  last_login INTEGER
);

-- 会话表
CREATE TABLE conversations (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL,
  name TEXT NOT NULL,
  type TEXT NOT NULL,
  extra TEXT NOT NULL,
  model TEXT,
  status TEXT CHECK(status IN ('pending', 'running', 'finished')),
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

-- 消息表
CREATE TABLE messages (
  id TEXT PRIMARY KEY,
  conversation_id TEXT NOT NULL,
  msg_id TEXT,
  type TEXT NOT NULL,
  content TEXT NOT NULL,
  position TEXT CHECK(position IN ('left', 'right', 'center', 'pop')),
  status TEXT CHECK(status IN ('finish', 'pending', 'error', 'work')),
  created_at INTEGER NOT NULL
);

-- 团队表
CREATE TABLE teams (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL,
  name TEXT NOT NULL,
  workspace TEXT NOT NULL,
  workspace_mode TEXT NOT NULL DEFAULT 'shared',
  lead_agent_id TEXT NOT NULL DEFAULT '',
  agents TEXT NOT NULL DEFAULT '[]',
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

-- 邮箱表（团队消息）
CREATE TABLE mailbox (
  id TEXT PRIMARY KEY,
  team_id TEXT NOT NULL,
  to_agent_id TEXT NOT NULL,
  from_agent_id TEXT NOT NULL,
  type TEXT NOT NULL DEFAULT 'message',
  content TEXT NOT NULL,
  summary TEXT,
  read INTEGER NOT NULL DEFAULT 0,
  created_at INTEGER NOT NULL
);

-- 团队任务表
CREATE TABLE team_tasks (
  id TEXT PRIMARY KEY,
  team_id TEXT NOT NULL,
  subject TEXT NOT NULL,
  description TEXT,
  status TEXT NOT NULL DEFAULT 'pending',
  owner TEXT,
  blocked_by TEXT NOT NULL DEFAULT '[]',
  blocks TEXT NOT NULL DEFAULT '[]',
  metadata TEXT NOT NULL DEFAULT '{}',
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);
```

### 2.2 索引设计

```sql
-- 用户索引
CREATE INDEX idx_users_username ON users(username);
CREATE INDEX idx_users_email ON users(email);

-- 会话索引
CREATE INDEX idx_conversations_user_id ON conversations(user_id);
CREATE INDEX idx_conversations_updated_at ON conversations(updated_at);
CREATE INDEX idx_conversations_type ON conversations(type);
CREATE INDEX idx_conversations_user_updated ON conversations(user_id, updated_at DESC);

-- 消息索引
CREATE INDEX idx_messages_conversation_id ON messages(conversation_id);
CREATE INDEX idx_messages_created_at ON messages(created_at);
CREATE INDEX idx_messages_type ON messages(type);
CREATE INDEX idx_messages_msg_id ON messages(msg_id);
CREATE INDEX idx_messages_conversation_created ON messages(conversation_id, created_at);

-- 团队索引
CREATE INDEX idx_teams_user_id ON teams(user_id);
CREATE INDEX idx_teams_updated_at ON teams(updated_at);

-- 邮箱索引
CREATE INDEX idx_mailbox_to ON mailbox(team_id, to_agent_id, read);

-- 任务索引
CREATE INDEX idx_tasks_team ON team_tasks(team_id, status);
```

---

## 三、Pragma 配置

```typescript
// 外键约束
db.pragma('foreign_keys = ON');

// 忙时超时（5秒），防止多进程并发锁定
db.pragma('busy_timeout = 5000');

// WAL 模式，提升性能
try {
  db.pragma('journal_mode = WAL');
} catch (error) {
  console.warn('[Database] Failed to enable WAL mode');
}
```

---

## 四、仓库接口设计

### 4.1 IConversationRepository

```typescript
export interface IConversationRepository {
  // 创建会话
  createConversation(params: CreateConversationParams): Promise<TChatConversation>;

  // 获取会话
  getConversation(id: string): Promise<TChatConversation | null>;

  // 更新会话
  updateConversation(id: string, updates: Partial<TChatConversation>): Promise<TChatConversation>;

  // 删除会话
  deleteConversation(id: string): Promise<void>;

  // 列出用户会话
  listConversations(userId: string, options?: ListOptions): Promise<TChatConversation[]>;

  // 搜索会话
  searchConversations(query: string, userId: string): Promise<TChatConversation[]>;

  // 检查会话是否繁忙
  isConversationBusy(id: string): Promise<boolean>;

  // 按 CronJob 获取会话
  getConversationsByCronJob(cronJobId: string): Promise<TChatConversation[]>;
}
```

### 4.2 流式消息缓冲 StreamingMessageBuffer

用于处理 AI 流式输出的消息缓冲：
- 缓冲增量文本
- 按阈值刷新到数据库
- 支持取消操作

---

## 五、tech-cc-hub 借鉴建议

### 5.1 可直接借鉴

| 功能 | AionUi 实现 | 移植价值 |
|------|-------------|----------|
| **仓库接口模式** | 抽象数据访问层 | 高 |
| **Schema 设计** | 完整的表结构参考 | 高 |
| **Pragma 配置** | WAL + busy_timeout | 高 |
| **索引策略** | 复合索引优化 | 中 |

### 5.2 我们的差异

| 差异点 | 说明 |
|--------|------|
| **多用户** | 我们无用户系统，简化为单用户 |
| **团队模式** | 我们无团队协作 |
| **ACP 协议** | 我们基于 Claude SDK |

### 5.3 Schema 对比

| 表 | AionUi | tech-cc-hub |
|----|--------|-------------|
| users | 有 | 无 |
| conversations | 有 | 有（需适配） |
| messages | 有 | 有（需适配） |
| teams | 有 | 无 |
| mailbox | 有 | 无 |
| team_tasks | 有 | 无 |
| cron_jobs | 外部表 | 需新增 |

### 5.4 适配建议

```sql
-- tech-cc-hub 简化 Schema
CREATE TABLE sessions (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  workspace TEXT,
  status TEXT CHECK(status IN ('active', 'archived')),
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

CREATE TABLE messages (
  id TEXT PRIMARY KEY,
  session_id TEXT NOT NULL,
  role TEXT NOT NULL,
  content TEXT NOT NULL,
  type TEXT NOT NULL,
  created_at INTEGER NOT NULL
);

CREATE TABLE events (
  id TEXT PRIMARY KEY,
  session_id TEXT NOT NULL,
  type TEXT NOT NULL,
  data TEXT NOT NULL,
  created_at INTEGER NOT NULL
);
```

---

**文档路径**: `doc/00-research/AionUi-调研报告/04-数据库层.md`
